{
	"name": "convention-automation",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"precommit": "lint-staged",
		"prepush": "npm test",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"lint-staged": {
		"*.js": [
			"prettier --write --traling-comma es5 --single-quote --tab-width 4", 
			"git add"
		]
	},
	"repository": {
		"type": "git",
		"url": "convention-automation"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"husky": "^1.3.1",
		"lint-staged": "^8.1.3",
		"prettier": "^1.16.3"
	}
}